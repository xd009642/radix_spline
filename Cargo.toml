[package]
name = "radix_spline"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
num-traits = "0.2.19"
serde = { version = "1.0", features = ["derive"], optional = true }

[dev-dependencies]
hegeltest = "0.29.3"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
